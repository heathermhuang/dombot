name: CI

# Runs typecheck, lint, and unit tests on every PR (and on pushes to main),
# and proves the self-hosted Worker still builds and bundles.

on:
  pull_request:
  workflow_dispatch:
  push:
    branches: [main, codex/hosted-foundation]

# Cancel superseded runs on the same ref (e.g. rapid pushes to a PR branch).
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        # --ignore-scripts skips Electron's ~150MB binary download (not needed
        # to typecheck, lint, or run the unit tests). Then rebuild esbuild so
        # Vite/Vitest have their native binary, whose install step
        # --ignore-scripts would otherwise skip.
        run: |
          npm ci --ignore-scripts
          npm rebuild esbuild
      - name: Typecheck
        run: npm run typecheck
      - name: Lint
        run: npm run lint
      - name: Unit tests
        run: npm test

  worker:
    name: Worker build (dry run)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: |
          npm ci --ignore-scripts
          npm rebuild esbuild
      - name: Build the web renderer
        run: npm run web:build
      - name: Bundle hosted gateway
        run: npx wrangler deploy --config hosted/wrangler.gateway.jsonc --dry-run --outdir dist/gateway
      - name: Bundle hosted tenant
        run: npx wrangler deploy --config hosted/wrangler.tenant.jsonc --dry-run --outdir dist/tenant
      - name: Bundle the Worker without deploying
        # No account or token needed: --dry-run stops after bundling.
        run: npx wrangler deploy --dry-run --outdir dist/worker
